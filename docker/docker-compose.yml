services:
  app:
    image: 'docker.io/best_insurance/api:latest'
    container_name: app
    depends_on:
      db:
        condition: service_started
    ports:
      - '8081:8080'
      - '5005:5005'
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/compose-postgres
      - SPRING_DATASOURCE_USERNAME=compose-postgres
      - SPRING_DATASOURCE_PASSWORD=compose-postgres
      - SPRING_JPA_HIBERNATE_DDL_AUTO=validate
      - BPL_DEBUG_ENABLED=true
      - BPL_DEBUG_PORT=5005
      - BPL_DEBUG_SUSPEND=false
      # FOARTE IMPORTANT PENTRU EROAREA DE LA OpenAPI
      - SPRINGDOC_API_DOCS_VERSION=OPENAPI_3_0
      - DATALOADER_LOADSAMPLE=true
      - EVENTLISTENER_QUEUE_NAME=subscriptionLogQueue
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER-URI=http://127.0.0.1:9090

  db:
    image: 'postgres:15.4-alpine'
    container_name: db
    ports:
      - '55432:5432'
    environment:
      - POSTGRES_USER=compose-postgres
      - POSTGRES_PASSWORD=compose-postgres
      - POSTGRES_DB=compose-postgres

  oauth:
    image: 'oauth2server:latest'
    build:
      context: .
      dockerfile: Dockerfile.oauth2TestServer
    container_name: oauth
    hostname: auth-server
    ports:
      - '9090:9090'